#include "nonceFinder.h"
#include <iostream>
#include <string>

const std::string processName = "Warframe.x64.exe";

/*int main() {
    std::cout << "Searching for process: " << processName << std::endl;
    std::string nonce = findNonceInProcess(processName);

    if (!nonce.empty()) {
        std::cout << "\nFound nonce: " << nonce << std::endl;
    } else {
        std::cout << "\nNo nonce found" << std::endl;
    }

    return 0;
}
*/